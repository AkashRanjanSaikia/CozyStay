/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["localhost" , "images.unsplash.com" ,"deih43ym53wif.cloudfront.net" ,"res.cloudinary.com" , "plus.unsplash.com",  ], // Add your backend host domain here
  },
};

export default nextConfig;
